# stark_lab 執行時相依（已精簡並升級至安全版本，2026-09-04）
#
# 變更說明（dependabot 弱點修補）：
#   - 移除未被程式 import 的舊套件（django-allauth / djangorestframework-jwt /
#     oauthlib / PyJWT / qrcode / psycopg2-binary / Pillow / pylint / astroid /
#     isort / virtualenv* 等）——這些是 dependabot 弱點大宗但實際未使用。
#   - Django 3.0.3 → 5.2 LTS（多個 CVE 修補；程式無破壞性 API，news 測試已驗證）。
#   - 補上實際使用卻漏列的 waitress（serve.py 正式啟動）與 python-dotenv（載入 .env）。
#   - 資料庫為 SQLite，不需 psycopg2。
#
# 部署機需 Python >= 3.10（Django 5.2 要求）。安裝：pip install -r requirements.txt

# --- Web 框架 ---
Django>=5.2,<5.3
djangorestframework>=3.15.2
django-filter>=24.3
django-cors-headers>=4.4.0
jsonfield>=3.1.0

# --- 設定 / 部署 ---
python-dotenv>=1.0.1
waitress>=3.0.0

# --- 資料抓取（news app：市場報價 / RSS 新聞 / 估值河流圖）---
requests>=2.32.3
pandas>=2.2.0
numpy>=1.26.0
yfinance>=0.2.60
feedparser>=6.0.11
beautifulsoup4>=4.12.3
